{
  "name": "clickraids",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "rsbuild dev --open",
    "build": "rsbuild build",
    "preview": "rsbuild preview"
  },
  "devDependencies": {
    "@rsbuild/core": "^1.0.0",
    "@rsbuild/plugin-sass": "^1.0.0",
    "typescript": "^5.5.2"
  },
  "dependencies": {
    "fzstd": "^0.1.1"
  }
}
